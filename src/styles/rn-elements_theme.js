import {Typography} from '../styles';

const theme = {
  Input: {
    containerStyle: {
      paddingHorizontal: 0,
    },
    inputStyle: {
      fontSize: Typography.FONT_SIZE_16,
    },
  },
};

export default theme;
